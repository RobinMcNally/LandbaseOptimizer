use support::types::CardSeries;
use support::types::Card;

fn randomcardset(setsize: i32) {

}

pub fn run(fixed: CardSeries, decksize: i32, deckcount: i32) {
    //generate base, random combindations of decks

    //repeat till whenever
    //
}
